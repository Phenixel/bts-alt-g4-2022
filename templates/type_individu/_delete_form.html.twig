<form method="post" action="{{ path('type_individu_delete', {'id': type_individu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_individu.id) }}">
    <button class="btn">Delete</button>
</form>
