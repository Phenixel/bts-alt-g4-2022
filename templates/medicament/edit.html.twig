{% extends 'base.html.twig' %}

{% block title %}Edit Medicament{% endblock %}

{% block body %}
    <a class="btn btn-primary" href="{{ path('medicament_index') }}"><i class="fas fa-arrow-left"></i> Revenir à la liste</a>

    <br><br>

    <h1>Editer Medicament</h1>

    {{ include('medicament/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
        {{ include('medicament/_delete_form.html.twig') }}
    </div>
{% endblock %}
