{% extends 'base.html.twig' %}

{% block title %}Interaction{% endblock %}

{% block body %}
    <a class="btn btn-primary" href="{{ path('prescrire_index') }}"><i class="fas fa-arrow-left"></i> Revenir à la liste</a>

    <br><br>

    <h1>Interaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interaction.id }}</td>
            </tr>
            <tr>
                <th>MED_PERTURBATEUR</th>
                <td>{{ interaction.MEDPERTURBATEUR }}</td>
            </tr>
            <tr>
                <th>MED_MED_PERTURBE</th>
                <td>{{ interaction.MEDMEDPERTURBE }}</td>
            </tr>
        </tbody>
    </table>

    <div class="list-btn">
        <a class="btn btn-secondary" href="{{ path('interaction_edit', {'id': interaction.id}) }}">Modifier</a>
    </div>
{% endblock %}
