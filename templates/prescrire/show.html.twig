{% extends 'base.html.twig' %}

{% block title %}Prescrire{% endblock %}

{% block activeP %}active{% endblock %}

{% block body %}
    <div class="header-table">
        <h1>Prescription</h1>
    </div>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ prescrire[0].id }}</td>
        </tr>
        <tr>
            <th>Médicament</th>
            <td>{{ prescrire[0].MED_NOMCOMMERCIAL }}</td>
        </tr>
        <tr>
            <th>Type individu</th>
            <td>{{ prescrire[0].tin_libelle }}</td>
        </tr>
        <tr>
            <th>Dosage</th>
            <td>{{ prescrire[0].DOS_QUANTITE }} {{ prescrire[0].DOS_UNITE }}</td>
        </tr>
        </tbody>
    </table>

    <div class="sup_table">
        <a class="btn btn-secondary" href="{{ path('prescrire_edit', {'id': prescrire[0].id}) }}">Modifier</a>
        {{ include('prescrire/_delete_form.html.twig') }}
    </div>
{% endblock %}